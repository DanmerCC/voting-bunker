name       = "voting-bunker"
pages_build_output_dir = "public"

# Compatibilidad mínima requerida por crypto.subtle en Workers
compatibility_date = "2024-01-01"

# Variables de entorno (añadir secretos reales via: wrangler secret put VAR)
[vars]
ENVIRONMENT = "production"
